import type { ClocksState, Duration, RelativeTime } from '@datadog/browser-core';
import type { LifeCycle } from '../lifeCycle';
import type { EventCounts } from '../trackEventCounts';
export declare const ACTION_CONTEXT_TIME_OUT_DELAY: number;
export type ActionCounts = EventCounts;
export interface TrackedAction {
    id: string;
    startClocks: ClocksState;
    duration: Duration | undefined;
    counts: ActionCounts;
    stop: (endClocks: ClocksState) => void;
    discard: () => void;
}
export interface ActionContexts {
    findActionId: (startTime?: RelativeTime) => string | string[] | undefined;
}
export interface ActionTracker {
    createTrackedAction: (startClocks: ClocksState) => TrackedAction;
    findActionId: (startTime?: RelativeTime) => string | string[] | undefined;
    stop: () => void;
}
export declare function startActionTracker(lifeCycle: LifeCycle): ActionTracker;
